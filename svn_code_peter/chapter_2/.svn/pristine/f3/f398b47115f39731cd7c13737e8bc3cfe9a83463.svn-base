#include <iostream>

using namespace std;

namespace sn {

    template <typename T>
    struct base
    {
	using value_type= T;
	T f(T i) { return (i + 3) * k; }
	T k{7};
    };

    // Wer so etwas schreibt, gehoert gefeuert!
    template <>
    struct base<short>
    {
	int
    return 0;
}
  
 
